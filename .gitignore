# WordPress plugin – dependencies & build artifacts

# PHP / Composer
/vendor/
composer.lock

# Node / npm (if tooling is added later)
node_modules/
/dist/

# WordPress upload cache
*.cache

# macOS
.DS_Store

# PHPUnit
.phpunit.result.cache
/coverage/
